language: node_js
node_js:
  - "10"
install:
  - npm install
  - npm run generate
script:
  - echo "Starting tests"
  # Test
  - npm run test
  # Run cypress tests
  - npm run cy:run
  # Stop development local server
  - kill $(job -p) || true
cache:
  directories:
    - node_modules
    - ~/.cache/Cypress
deploy:
  provider: pages
  skip-cleanup: tru
  github-token: $GITHUB_TOKEN
  target-branch: gh-pages
  local-dir: dist
  on:
    branch: master